diff --git a/python/sglang/srt/entrypoints/http_server.py b/python/sglang/srt/entrypoints/http_server.py
index fc9f5f0cf8..98c0b5afa4 100644
--- a/python/sglang/srt/entrypoints/http_server.py
+++ b/python/sglang/srt/entrypoints/http_server.py
@@ -1823,7 +1823,10 @@ async def openai_v1_realtime_transcription(ws: WebSocket):
 @app.get("/v1/models", response_class=ORJSONResponse)
 async def available_models():
     """Show available models. OpenAI-compatible endpoint."""
-    served_model_names = [_global_state.tokenizer_manager.served_model_name]
+    served_model_names = list(
+        getattr(_global_state.tokenizer_manager, "served_model_aliases", None)
+        or [_global_state.tokenizer_manager.served_model_name]
+    )
     model_cards = []
 
     # Add base model
@@ -1855,7 +1858,10 @@ async def available_models():
 @app.get("/v1/models/{model:path}", response_class=ORJSONResponse)
 async def retrieve_model(model: str):
     """Retrieves a model instance, providing basic information about the model."""
-    served_model_names = [_global_state.tokenizer_manager.served_model_name]
+    served_model_names = list(
+        getattr(_global_state.tokenizer_manager, "served_model_aliases", None)
+        or [_global_state.tokenizer_manager.served_model_name]
+    )
 
     if model not in served_model_names:
         return ORJSONResponse(
diff --git a/python/sglang/srt/managers/tokenizer_manager.py b/python/sglang/srt/managers/tokenizer_manager.py
index 41df6be03d..3ffb05c621 100644
--- a/python/sglang/srt/managers/tokenizer_manager.py
+++ b/python/sglang/srt/managers/tokenizer_manager.py
@@ -453,6 +453,15 @@ class TokenizerManager(TokenizerControlMixin, TokenizerManagerScoreMixin):
         # Read model args
         self.model_path = server_args.model_path
         self.served_model_name = server_args.served_model_name
+        # [sm120 patch 0008] Comma-separated --served-model-name entries become
+        # aliases: all names are listed on /v1/models and accepted by
+        # /v1/models/{id}; served_model_name stays the canonical (first) name
+        # for every other consumer. Chat/completions accept any name because
+        # request.model is not validated against served_model_name.
+        _raw_alias_names = (server_args.served_model_name or "").split(",")
+        self.served_model_aliases = [
+            _n.strip() for _n in _raw_alias_names if _n.strip()
+        ] or [self.served_model_name]
         self.model_config = model_config_class.from_server_args(server_args)
         self.is_generation = self.model_config.is_generation
         self.context_len = self.model_config.context_len
